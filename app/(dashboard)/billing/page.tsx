import { CheckCircle, XCircle } from "lucide-react";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

const BillingPage = async () => {
  return (
    <div className="container mx-auto py-12 flex items-center justify-center"></div>
  );
};

export default BillingPage;
